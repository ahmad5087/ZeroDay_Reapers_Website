node_modules/
.next/
out/
.env*
.vercel
.DS_Store
*.log

# Scope B files stay available locally but are never published to GitHub.
*.md
/.env.example
/.env.staging.example
/pi/backup/backup.env.example
/pi/monitor/gatus.yaml

# large source avatars — only the resized public/avatars/*.webp are committed
app/portal/avatar/

# local-only internal docs — keep out of the public repo
*.pdf
ZeroDay Reapers Google Classroom Link.txt

# Pi runtime secrets/state — never commit (only the *.example templates belong in the repo)
gatus.env
backup.env
rclone.conf
gatus.db
restic-cache/
restic-stage/
